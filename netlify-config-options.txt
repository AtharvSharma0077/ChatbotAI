# Option 1: With base directory (RECOMMENDED)
[build]
  base = "frontend"
  command = "yarn install && yarn build"
  publish = "build"

[build.environment]
  NODE_VERSION = "18"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

---

# Option 2: Without base directory
[build]
  command = "cd frontend && yarn install && yarn build"
  publish = "frontend/build"

[build.environment]
  NODE_VERSION = "18"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

---

# Use Option 1 (top one) - It's already active in netlify.toml
